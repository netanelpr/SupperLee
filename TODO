Remove the attributes in the Supplier.SupplierSystem because there is already connetion
with those classes

Do the TODOS in the code.

Consider adding an Result<T> in order to create a better code and 
	notify the upper layers with error details if one accord
